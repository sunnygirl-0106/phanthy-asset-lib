# 资产库 Demo 修改 · 技术文档（交付 Claude Code）

> 仓库：asset-lib-demo（React + TS + Zustand，逻辑内核在 `src/services/`，状态在 `src/store/useStore.ts`）
> 本文档由产品逻辑评审收敛而来。**分三个阶段施工，按顺序做，每阶段做完跑 `npm run typecheck` + `npm test` 再进下一阶段。**
> 总原则：本版只改「界面表达 + 演示所需的最小逻辑」，**不动权限体系（permission.ts 不改）**，第二节的后端规则仅作记录、不要求 demo 实现。

---

## 一、产品规则总表（Rules of Record）

以下规则是产品口径的最终定稿。标 ✅ 的需要本版 demo 实现；标 📋 的是后端/后台规则，只需记录，demo 不实现。

### R1. 图片删除的分层语义 ✅

一份资产的"图片集合" = 详情页可见的全部图片（角色：素模 + 全部造型；服装/场景/道具：本体图 + 全部样式）。每张图片都可以单独删除。当一次删除会使图片数**归零**时，按资产所在层分流：

| 层 | 行为 | 提示文案 |
|---|---|---|
| 素材广场 | **本版不做**（广场删除仅 admin 下架，维持现状） | — |
| 团队资产库 | 弹确认 → 确认后**删除整个资产**（含提示词、音色） | 「这是该资产的最后一张图片，继续将删除整个资产，提示词会一并删除。」 |
| 项目资产库 | 弹提示 → 确认后**图片清空、保留空壳**：资产降级为 `status:'empty'`，保留名字/提示词/音色/血缘 | 「图片将清空，提示词会保留，你可以随时重新生成。」 |

空壳（empty）资产：
- 卡片与详情显示空图占位符；
- 详情里点**提示词图标**进入提示词面板，可编辑提示词并点「重新生成」恢复为成品（demo 占位：落占位图 + `status:'done'`）；
- 空壳**不可流转**——`assetService.assertDone` 红线已天然保证（empty ≠ done），无需新代码；
- 空壳的占位图上保留一个垃圾桶 = 删除整个空壳资产（提示「将删除该资产及提示词」）——这是空壳唯一的整体删除出口；
- **不做**"从库复用填充空壳"（产品确认无此逻辑，永不引入，避免重复资产）。

### R2. 详情页封面大图不再承载"删除整个资产" ✅

封面大图 hover 只保留：放大查看、下载原图、提示词。删除动作全部下沉到图片卡级别（语义一律是"删这张图"，配合 R1 分流）。

### R3. 音频为一等展示类目 ✅

- 团队库、项目库补"音频"类目入口；
- 展示样式沿用画布资产面板的**条状行**（试听 ▶ + 名称 + 波形占位 + 时长）；
- 音频行仅支持两个操作：**删除、编辑名称**。不做音频详情页、不做沉淀/贡献流转（本版音频进了哪层留在哪层）；
- 音频不再提供「关联已有」保存方式（挂到另一段音频的 looks 下无处展示，语义废弃）。

### R4. 画布音频保存 = 二选一 ✅

画布音频节点右键「保存到资产库」不再走类目瓷砖弹窗，改为用途二选一：

```
把这段音频保存为——
① 音频素材   台词 / BGM / 音效，存入项目资产库（可选：关联到某个角色）
② 角色音色   用这段声音作为某个角色的音色（复刻样本，需 5–10s 清晰人声）
```

- 选①：填名称 → 落项目库 audio 类目（现有 `runSaveToProject`，mode 恒为 new）；「关联角色」为可选下拉（本项目角色列表），选了则存 `fields.roleId`，音频行尾部显示小角色标签；
- 选②：第二步选角色（本项目角色列表）→ 复用现有克隆音色数据结构（`Voice{type:'cloned'}`，previewUrl/sampleUrl = 节点音源）→ `setVoice` 落到该角色；角色已有音色时提示「将替换现有音色「××」」；
- 防呆：若该音频节点的血缘（`node.source`）指向的资产正是所选角色，提示「这段音频由该角色的音色生成，无需再设为音色」（demo 可降级为不做，标 TODO）。

### R5. 音色拷贝规则 📋（现状已符合，无代码改动）

直接复用 / 收藏 / 复用 / 沉淀四条流转线：只要源资产存在音色，一律允许拷贝、默认携带。现有实现（`cloneForCopy` 深拷贝 voice + 直接复用的 includeVoice 勾选）已满足，保持现状。

### R6. 子账号禁用/注销后的广场素材保留 📋（后端规则，demo 不实现）

- 子账号被禁用或注销后，其贡献到广场的素材**必须保留**，正常陈列；
- 原「投稿作者可删自己投稿」通道随账号失效一并关闭，此后该素材的移除权仅归 admin；
- `contributedBy` 字段封存不清空（保留追溯）。

---

## 二、阶段一：音频补全

**目标**：团队库、项目库出现音频类目，条状行展示，支持删除与改名。

**涉及文件**：
- `src/pages/TeamLibraryPage.tsx`（CATEGORIES 边栏，L23-28）
- `src/components/ProjectAssetLibrary.tsx`（CATEGORIES 顶部 Tab，L22-27）
- `src/components/canvas/CanvasAssetPanel.tsx`（音频行的现成实现：`renderAudioList` L185-222、WAVE_BARS、audioSrcOf/audioDurationOf，抽取复用）
- `src/store/useStore.ts`（复用现有 `runDeleteAsset` / `renameAsset`，无需新 action）
- `src/data/seed.ts`（已有 audio 种子数据，含 fields.audioUrl / duration，直接可用）

**施工说明**：
1. 把 `CanvasAssetPanel` 里的音频行（含播放态管理、波形占位、样式）抽成共享组件 `src/components/AudioRow.tsx`（或 `AudioList.tsx`），画布面板改为引用它，保证两处视觉一致；
2. 两个库页的 CATEGORIES 数组加 `{ key: 'audio', label: '音频', icon: <占位icon> }`；icon 链接产品稍后提供，先用现有任一 icon 或内联 SVG 占位，代码里留 `// TODO: 替换音频 icon` 注释；
3. 选中音频类目时，右侧内容区不渲染 AssetCard 网格，改渲染音频行列表；
4. 库页音频行行尾加两个 hover 操作：
   - **改名**：铅笔图标 → 行内 input，Enter 提交，走现有 `renameAsset`（自带库内同名去重）；
   - **删除**：垃圾桶图标 → 走现有 `runDeleteAsset`；
5. 音频行**不**接入 AssetDetail（点击行不打开详情）；批量操作模式下音频类目可暂不支持勾选（保持简单）。

**验收**：
- [ ] 团队库 / 项目库各出现「音频」类目，计数角标正确（两页的 counts 已统计 audio，只是没展示）；
- [ ] 音频行可试听、可改名（重名被挡）、可删除；
- [ ] 画布资产面板音频行为不回归（试听/使用/拖拽照旧）；
- [ ] `npm test` 全绿。

---

## 三、阶段二：删除逻辑分层

**目标**：落实 R1 + R2。

**涉及文件**：
- `src/components/AssetDetail.tsx`（封面大图垃圾桶 L859-868；素模卡垃圾桶 L925-934；造型卡垃圾桶 L949-958）
- `src/services/assetService.ts`（`removeLook` L81-96）
- `src/store/useStore.ts`（`runDeleteLook` L444-462；新增一个空壳化 action）
- `src/components/AssetCard.tsx`（空壳态占位视觉）
- `src/data/types.ts`（无需改，`AssetStatus` 已含 `'empty'`）

**施工说明**：
1. **移除封面大图的删除入口**：删掉 `AssetDetail` 库页布局中封面大图上的 `cardDelete` 按钮及素模小卡上那个同样调用 `deleteWholeAsset` 的按钮；`deleteWholeAsset` 仅保留给空壳态与广场（admin 下架维持现状）；
2. **图片卡删除统一为"删这张图"**：右侧网格中每张图片卡（含素模卡）hover 垃圾桶语义统一为删除单张；素模的"单张删除"实现为：清 `baseModel`（角色场景下素模被删仅在它是最后一张图时才允许——非最后一张时素模删除本版可以不做，加 title 提示「素模为角色本体，删除其他图片后可随资产一并处理」，避免改动过大）；
   - 最小实现口径：**造型/样式卡逐张可删（现状），素模/本体图只在"它是最后一张"时可删**，此时删除动作即触发第 3 步的归零分流；
3. **归零分流**（改 `runDeleteLook` 或在 AssetDetail 层拦截）：删除动作执行前判断"删完后图片数是否为 0"（角色：`!baseModel && looks为空`；其他类目：`looks为空 && 这张是本体图`）：
   - `scope === 'team'` → `window.confirm('这是该资产的最后一张图片，继续将删除整个资产，提示词会一并删除。')` → 确认则 `runDeleteAsset`；
   - `scope === 'project'` → `window.confirm('图片将清空，提示词会保留，你可以随时重新生成。')` → 确认则调用新 action `clearAssetImages(assetId)`；
   - `scope === 'plaza'` → 不改现状；
   - demo 用 `window.confirm` 即可，不必做样式化弹窗；
4. **新增 store action `clearAssetImages(assetId)`**：不可变更新该资产为 `{ ...a, status: 'empty', cover: '', baseModel: undefined, looks: undefined }`，**保留 name / prompt / voice / masterId / fields / tags**；
5. **空壳视觉**：
   - `AssetCard`：`status === 'empty'` 时封面区渲染空图占位（虚线框 + 图标 + 「待生成」字样即可）；
   - `AssetDetail`：空壳态左栏大图显示占位符，占位符上保留一个垃圾桶（`deleteWholeAsset`，title「删除该资产（提示词将一并删除）」）；右栏造型网格为空；提示词图标照常可点，提示词面板里的「重新生成」按钮（现有 `regenerateBaseModel`）执行后：`status → 'done'`、cover 落占位图（复用 `LOOK_PLACEHOLDER`）——即"再次生成"恢复成品；
6. **流转红线自检**：空壳资产点「存入团队库 / 贡献到广场」应被 `assertDone` 挡下并显示错误信息——已有机制，写一条测试覆盖即可。

**验收**：
- [ ] 详情页封面大图 hover 无垃圾桶，仅 放大/下载/提示词；
- [ ] 团队库资产删到最后一张：确认弹窗 → 整资产消失；
- [ ] 项目库资产删到最后一张：确认弹窗 → 空壳卡片（占位图），名字与提示词保留；
- [ ] 空壳 → 提示词面板 → 重新生成 → 恢复成品（占位图）；
- [ ] 空壳不能沉淀/贡献（有错误提示）；
- [ ] 新增测试：clearAssetImages 保留 prompt/voice、empty 不可流转、团队库归零删整资产；`npm test` 全绿。

---

## 四、阶段三：画布音频保存交互

**目标**：落实 R4。

**涉及文件**：
- `src/components/UploadToLibraryModal.tsx`（现有保存弹窗，仅图片节点继续使用）
- 新增 `src/components/SaveAudioModal.tsx`（音频节点专用二选一弹窗）
- `src/pages/CanvasShell.tsx`（右键「保存到资产库」按 `node.media` 分流两个弹窗）
- `src/store/useStore.ts`（复用 `runSaveToProject` 与 `setVoice`，无需新 action）
- `src/services/canvasService.ts`（音频不再允许 link：`saveCanvasNodeToProject` 对 `media==='audio' && mode==='link'` 抛 AssetRuleError）

**施工说明**：
1. `CanvasShell` 中触发保存时：`node.media === 'audio'` → 打开 `SaveAudioModal`；否则维持现有 `UploadToLibraryModal`（图片流程零改动）；
2. `SaveAudioModal` 第一步：两张大选项卡（文案见 R4）；
3. 选「音频素材」：显示 名称输入（必填）+「关联角色（可选）」下拉（数据源：本项目 `scope==='project' && category==='character'` 的资产）→ 确认调 `runSaveToProject(node, pid, { category:'audio', mode:'new', name })`；若选了角色，落库后把 `fields.roleId` 写入（可在 spec 里扩展一个可选字段透传，或落库后补一次不可变更新，实现从简）；音频行尾部显示角色名小标签；
4. 选「角色音色」：显示角色下拉（同上数据源）→ 所选角色已有音色时在确认按钮上方提示「将替换现有音色「{voice.name}」」→ 确认构造 `Voice { id: 'cloned_'+时间戳, type:'cloned', name: 节点名 或用户输入, previewUrl: node.content, sampleUrl: node.content }` 调 `setVoice(角色id, voice)`，反馈「已设为「{角色名}」的音色，复刻将在接入语音模型后生效」；此路径**不产生** audio 资产；
5. `UploadToLibraryModal` 中音频类目瓷砖可以整块移除（音频不再走它）。

**验收**：
- [ ] 音频节点右键保存出现二选一弹窗；图片节点弹窗不变；
- [ ] 存为音频素材 → 项目库音频 Tab 可见（依赖阶段一）、可选角色标签正确显示；
- [ ] 设为角色音色 → 角色详情音色栏更新、替换提示正确出现；不新增音频资产；
- [ ] 音频走 link 被 service 层拒绝（补一条测试）；`npm test` 全绿。

---

## 五、本版明确不做（防止顺手扩大范围）

1. 广场的删除/归零逻辑（admin 下架维持现状）；
2. 空壳的「从库复用填充」——产品确认**永不引入**，任何"复用进项目库"动作只从团队库/广场资产详情发起；
3. 删除二次确认的样式化弹窗（`window.confirm` 足够）；
4. 权限体系调整（子账号删团队库、投广场审批链等，`permission.ts` 零改动）；
5. 审核快照化、通知补全、重复申请防护；
6. 音频详情页、音频沉淀/贡献流转、批量操作实动作。

## 六、待补事项

- 音频类目 icon：产品后续提供链接，代码中以 `// TODO: 替换音频 icon` 标记占位处。
