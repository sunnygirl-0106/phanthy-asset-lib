# 技术文档 · 新增「其他」类目（项目资产库）

> 仓库：asset-lib-demo。本文档只覆盖**这一个改动**，此前的音频补全 / 删除分层 / 音频保存二选一均已实现，不要重做。
> 施工前先读 `src/data/types.ts`（活文档）与 `src/services/canvasService.ts`。做完跑 `npm run typecheck` + `npm test`。

---

## 一、这是什么、为什么加

用户在画布上创作时，除了角色 / 服装 / 场景 / 道具 / 音频这些**可复用的生产要素**，还会产出一批**创作过程的留存物**：分镜图、视频片段、剧本或台词文本。这些东西目前无处安放——画布上的视频节点和文本节点根本不允许入库（`categoriesForMedia` 对 video / text 返回空数组）。

本次新增「其他」类目来接住它们。

**核心定位（务必理解，后面所有细节都由它推导）**：

> 「其他」不是第 6 个平级类目，它是**另一层东西**。
> 前五类 = 可复用的生产要素，存进来是为了以后拿去生成新内容；
> 「其他」 = 创作过程的留存物，存进来是为了备查、备用、随时拖回画布。

由此推导出三条硬约束：

1. **只在项目资产库出现。** 团队库不加、素材广场不加。理由：团队库是"跨项目常驻母版库"，分镜图天然项目专属，第三场第五镜的分镜换个剧本毫无意义；开了这个口团队库会从"货架"退化成"网盘"。
2. **不参与任何向上流转。** 「其他」资产不能"存入团队库"、不能"贡献到素材广场"。这与第 1 条互为因果——既然团队库/广场没有这个类目，自然也没有流入路径。
3. **不套用前五类那套资产结构。** 没有素模、没有造型/样式子资产、没有封面选择、没有音色、没有提示词重新生成。

---

## 二、数据模型改动

### 2.1 Category 增加 `'other'`

`src/data/types.ts`：

```ts
export type Category = 'character' | 'costume' | 'scene' | 'prop' | 'audio' | 'other'
//                      角色         服装         场景       道具     音频      其他
```

改完后所有 `Record<Category, ...>` 的地方会报红（这正是活文档的价值），逐个补 `other` 分支即可。已知需要补的：
- `src/components/AssetCard.tsx` → `CATEGORY_LABEL`
- `src/components/AssetDetail.tsx` → `CATEGORY_LABEL`
- `src/components/UploadToLibraryModal.tsx` → `CAT_LABEL`
- `src/components/ProjectAssetLibrary.tsx` / `src/pages/TeamLibraryPage.tsx` → `counts` 初始化对象
- `src/data/seed.ts` → `PROMPT_BY_CATEGORY`（「其他」无提示词，给空串）

### 2.2 用 `fields` 承载媒介信息，不新增顶层字段

「其他」下面混着三种媒介，需要知道每一份到底是图、是视频还是文本。沿用音频那套做法（音频把 `audioUrl` / `duration` 塞在 `fields` 里），不动 `Asset` 顶层结构：

| fields 键 | 类型 | 说明 |
|---|---|---|
| `media` | `'image' \| 'video' \| 'text'` | 「其他」资产必填，决定卡片和详情怎么渲染 |
| `videoUrl` | `string` | 仅 `media==='video'`，可播放视频源（demo 可为空字符串占位） |
| `text` | `string` | 仅 `media==='text'`，正文内容 |

- `media==='image'`：`cover` 直接存图片地址（与现有类目一致）；
- `media==='video'`：`cover` 存视频首帧/海报图；
- `media==='text'`：`cover` 留空，卡片渲染文字预览块，不出现裂图。

在 `types.ts` 的 `AssetFields` 注释里补一句说明即可，无需改类型（已有索引签名）。

---

## 三、界面改动

### 3.1 项目资产库加「其他」Tab

`src/components/ProjectAssetLibrary.tsx`：`CATEGORIES` 数组末尾追加

```ts
{ key: 'other', label: '其他', icon: assetUrl('assets/icons/cat-other.svg') },
```

**视觉要求：把「其他」放在类目 Tab 最右侧，且与前五类之间加一条竖分隔线（或明显的间距）**，暗示"这不是同一层的东西"。否则用户会默认它和角色/场景平级，然后来问"其他为什么不能沉淀到团队库"。

icon 暂用占位（可复用 `image.svg` 或 `grid.svg`），代码里留 `// TODO: 替换「其他」icon`。

**团队库 `TeamLibraryPage.tsx` 的 CATEGORIES 不要动**，保持五类。

### 3.2 「其他」的卡片网格

选中「其他」Tab 时仍走 `AssetCard` 网格（不像音频那样换成条状行），但卡片按 `fields.media` 分三种渲染：

- **图片**：与现有卡片一致，满幅封面；
- **视频**：封面 + 中央半透明播放三角 + 右下角时长（若有）；
- **文本**：不放图，卡面渲染深色底 + 前 3～4 行正文预览（超出省略号）。

三种都在左上角打一个媒介小徽章（`图片` / `视频` / `文本`），让用户一眼分得清。

### 3.3 「其他」的详情

点开仍复用 `AssetDetail`，但对 `asset.category === 'other'` 走**精简模式**，需要隐藏的部分：

| 区块 | 「其他」是否显示 |
|---|---|
| 标题 + 改名 ✎ | ✅ 显示 |
| 类目 / 所在层 chips | ✅ 显示 |
| 「存入团队库」按钮 | ❌ 隐藏 |
| 「贡献到素材广场」按钮 | ❌ 隐藏 |
| 左栏大图 | ✅ 显示（视频→播放器占位；文本→正文全文滚动区） |
| 放大查看 / 下载原图 | ✅ 显示（文本类只留"复制全文"） |
| 音色区块 | ❌ 隐藏（本来就只有角色有） |
| 右栏「其他造型 / 其他样式」网格 | ❌ 整块隐藏 |
| 「＋新增造型」按钮 | ❌ 隐藏 |
| 提示词角标 / 重新生成 | ❌ 隐藏（「其他」是存进来的成品，不由提示词生成） |
| 删除 | ✅ 显示（语义见 3.4） |

实现建议：在组件里取一个 `const isOther = asset.category === 'other'`，用它把上述区块条件渲染掉，不要新写一个组件。

### 3.4 删除语义：「其他」不留空壳

这是与前五类的关键差异，别照抄项目层的空壳逻辑：

> **「其他」资产没有提示词、不能重新生成，所以删除就是删除，直接从项目库移除，不保留空图占位。**

走现有 `runDeleteAsset` 即可。删除入口放在详情页左栏大图上（这是「其他」唯一的整体删除出口），以及卡片 hover。

---

## 四、画布侧改动

### 4.1 放开 video / text 入库

`src/services/canvasService.ts`：

```ts
export function categoriesForMedia(media: Media): Category[] {
  if (media === 'image') return ['character', 'costume', 'scene', 'prop', 'other']
  if (media === 'audio') return ['audio']
  if (media === 'video') return ['other']
  if (media === 'text') return ['other']
  return []
}
```

改完之后视频节点、文本节点的右键「保存到资产库」自动可用（`canUploadToProject` 依赖这个函数），`NodeContextMenu` 不用动。

### 4.2 保存弹窗

`src/components/UploadToLibraryModal.tsx`：

1. `TILE_ORDER` 追加 `'other'` → `['character', 'prop', 'costume', 'scene', 'other']`；同样在视觉上把「其他」与前四个隔开；
2. **「其他」只支持「新建」，不支持「关联已有」**——它没有子资产概念。点中「其他」瓷砖后直接视为 `mode:'new'`，不展开保存方式浮层；
3. 落库时把媒介写进 fields：`media` 取自 `node.media`；`node.media==='text'` 时把 `node.content` 存进 `fields.text`；`node.media==='video'` 时把音源/视频地址存进 `fields.videoUrl`。

建议在 `SaveSpec` 上加一个可选透传字段（如 `extraFields?: AssetFields`），由 `saveCanvasNodeToProject` 合并进产出资产的 `fields`，避免落库后再补一次更新。

### 4.3 服务层守卫

`saveCanvasNodeToProject` 里补两条校验：
- `spec.category === 'other' && spec.mode === 'link'` → 抛 `AssetRuleError('「其他」类目不支持关联到已有资产。')`
- 现有的媒介↔类目相容性校验会自动生效（video/text 只能落 other），无需额外写。

### 4.4 画布左侧资产面板

`CanvasAssetPanel.tsx` 的 `CATEGORY_TABS` 追加「其他」，让用户能把存下来的分镜图重新拖回画布。注意：
- `mediaOf(asset)` 目前只区分 audio / image，需要改成读 `fields.media`（其他类）→ 返回对应 Media；
- 「其他」资产不进 `STYLE_CATS`（无造型），单图直接「使用」；
- 文本类「其他」点「使用」落文本节点（`media:'text'`，`content` 取 `fields.text`）。

---

## 五、流转与权限守卫

在服务层挡死，不要只靠界面隐藏按钮：

1. `assetService.deposit()` 与 `materializeDeposit()`：`source.category === 'other'` → 抛 `AssetRuleError('「其他」类目仅存在于项目资产库，不能沉淀到团队库。')`
2. `assetService.contributeToPlaza()`：同上，抛 `AssetRuleError('「其他」类目不能贡献到素材广场。')`
3. `permission.ts` **不需要改**——这是类目能力约束，不是角色权限，放在 assetService 更合适。

---

## 六、种子数据

三张新图已放进 `public/assets/proj-neon-tokyo/`，命名沿用仓库现有规范：

- `mission_storyboard_other.png` —— 九宫格任务分镜（天台接警 → 任务完成离开）
- `rainy_street_storyboard_other.png` —— 雨夜街头九宫格分镜（镜头 01–09）
- `full_plot_board_other.png` —— 完整情节故事板（第一幕 → 黎明收束）

在 `src/data/seed.ts` 里给「霓虹东京」项目（`IDS.projNeon`）加 4 份「其他」资产，**三种媒介都要覆盖**，这样演示时能一次讲清：

| 名称 | media | 素材 |
|---|---|---|
| 任务线·九宫格分镜 | image | `mission_storyboard_other.png` |
| 雨夜街头·分镜片段 | video | 封面用 `rainy_street_storyboard_other.png`，`videoUrl` 留空占位 |
| 完整情节故事板 | image | `full_plot_board_other.png` |
| 第一幕剧本·雨夜追踪 | text | 无图，`fields.text` 写一段 200 字左右的剧本文本 |

建议照着现有 `audioAsset()` 的写法，加一个 `otherAsset(id, name, scopeId, media, coverOrText)` 小助手，保持 seed 文件的风格一致。

---

## 七、验收清单

- [ ] 项目资产库出现「其他」Tab，位于最右侧且有视觉分隔，计数正确；团队库和素材广场**没有**这个类目
- [ ] 「其他」网格里图片 / 视频 / 文本三种卡片渲染正确，各带媒介徽章，文本卡不出现裂图
- [ ] 点开「其他」详情：无"存入团队库"、无"贡献到素材广场"、无造型网格、无音色、无提示词入口
- [ ] 删除「其他」资产 = 直接移除，**不产生空壳**
- [ ] 画布上视频节点、文本节点右键「保存到资产库」可用，图片节点可选「其他」
- [ ] 「其他」在保存弹窗里只有"新建"，选不到"关联已有"
- [ ] 存下来的「其他」资产能在画布左侧面板里找到并拖回画布（文本落文本节点）
- [ ] 服务层守卫生效：对 other 调用 deposit / contributeToPlaza 抛业务错误（补 2 条测试）
- [ ] `npm run typecheck` 与 `npm test` 全绿

## 八、本次不做

- 团队资产库、素材广场的「其他」入口（明确不加）
- 真实视频播放（`videoUrl` 留空占位即可）
- 「其他」的批量操作实动作
- 团队级"参考资料库"（将来若有片头模板 / 运镜参考的需求，那是独立功能，不是把项目的「其他」往上沉淀）
