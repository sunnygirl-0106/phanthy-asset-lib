# 资产库 Demo · 产品逻辑评审

> 评审范围：asset-lib-demo 全部逻辑层代码（types / permission / assetService / canvasService / useStore）+ 各页面组件。
> 结论按「P0 硬伤 → P1 逻辑缺口 → 三个待决问题的方案 → 口径小错位」组织。每条都标了代码出处，方便对着改。

---

## 一、P0 硬伤（逻辑上自相矛盾，必须先定）

### 1. 音频是一个「进得去、看不见、动不了」的黑洞

这比"团队库/项目库忘了放音频 Tab"严重。串起来看整条链：

- 画布右键"保存到资产库"**允许**存成音频（`UploadToLibraryModal`，audio 是合法类目；`canvasService.categoriesForMedia('audio') = ['audio']`）；
- 但项目资产库页面**没有音频 Tab**（`ProjectAssetLibrary.tsx` 第 22 行 CATEGORIES 只有四类）——**用户存成功了，却在库里找不到它**，只有回到画布左侧资产面板才能看见；
- 音频资产在任何地方都**打不开详情**（画布面板的音频行只有"试听/使用/拖拽"，没有进详情的入口；库页又没有 Tab）——所以音频**不能改名、不能沉淀到团队库、不能贡献到广场、不能删除**。它进了项目库就永远困在项目库；
- 团队库/广场同样没有音频 Tab（你已知），意味着即使补了沉淀入口，沉淀上去也一样看不见；
- 另外"保存到资产库"弹窗对音频也提供了**「关联已有」**——把一段音频挂到另一段音频的 `looks[]` 下。音频没有"造型/样式"概念，挂上去之后**没有任何界面会展示它**，等于数据静默丢失。

**建议**：把"音频"当一等类目补全整条链（三层库 Tab + 音频详情页/详情行 + 流转动作），或者本期明确降级为"音频只存在于画布面板"，把保存弹窗里的音频入口一并收掉——两头都开一半是最差的状态。音频的「关联已有」建议直接去掉（或改语义为"关联到角色"，见问题三）。

### 2. 删除无二次确认，且"删一张图"和"删整个资产"两个按钮长得一模一样

- 详情页**封面大图上的垃圾桶是"删除整个资产"**（`AssetDetail.tsx` L859-868 `deleteWholeAsset`），造型小卡上的垃圾桶是"删除该造型"（L949-958）。图标、样式完全相同，位置只差一个层级——用户以为删一张图，实际整个角色（连同提示词、音色、全部造型）瞬间没了；
- `runDeleteAsset` / `runRemovePlaza` / `runDeleteLook` **全部没有二次确认**，删完不可恢复（store 直接 filter 掉）；
- 团队库工具条有"批量操作"，但只有选择态，没有批量删除/批量流转动作——若将来接上批量删除，叠加上面两条会更危险。

**建议**：所有删除加确认；"删除整个资产"从图片 hover 里挪出来，放到详情头部溢出菜单（⋯）里，与"删除这张图"在视觉与位置上强区分。这也是你问题一的前置（见第三节）。

### 3. 权限倒挂：子账号写团队库要审批，删团队库却畅通无阻

`permission.ts` L184-187：

```ts
export function canDeleteLibraryAsset(user, asset) {
  if (asset.scope !== 'team' && asset.scope !== 'project') return false
  return !isAdmin(user)   // ← owner / sub 一律可删
}
```

- 子账号沉淀进团队库要走"申请→主账号批"，但**直接删除团队库母版不需要任何审批**，且叠加硬伤 2（无确认、按钮同形），一个子账号可以静默清空团队库；
- 该函数也**不校验团队归属和项目分配**（不看 `user.teamId === asset.scopeId`）。现在 UI 因为 canSee 挡住了看不见的资产所以出不了事，但你们的架构原则是"这些规则将来原样搬后端做鉴权"——搬过去就是越权漏洞。

**建议**：删除权限至少对齐写入权限：子账号不能删团队库资产（或同样走申请）；所有写/删函数都补上归属校验。项目库内是否允许子账号删"别人沉淀的资产"，也值得定一条规则（现在项目库内任何成员可删任何资产）。

### 4. 审核是"引用"不是"快照"，批的可能不是提交的那份

- `approveApplication` / `approvePlazaSubmission` 落库时用 `findAsset(world, appl.assetId)` 取**当前最新状态**（`useStore.ts` L305-309、L370-373）。子账号提交沉淀申请后继续改这个资产（改提示词、加造型、换音色），主账号看到的申请卡片只有名字，点"通过"时入库的是**改过之后的版本**——审核形同虚设；
- 源资产在审核期间被删（画布/详情都能删），申请变成永远批不掉的死单：approve 返回"原资产已不存在"，但申请仍停在 pending，只能靠驳回清掉；
- 审核中心的申请卡片**只有资产名和来源文字，没有图**，主账号/admin 实际上是在盲批（`ReviewCenterPage.tsx` Card 组件）；
- 驳回没有理由字段，申请人只收到一句"被驳回"，不知道该改什么。

**建议**：申请生成时冻结快照（至少封面图 + 造型勾选 + 提示词摘要），审核界面展示快照并可点开预览；源资产删除时级联作废相关 pending 申请；驳回加选填理由。

### 5. 对外免审、对内要审：子账号投广场绕过了主账号

现在的把关强度是倒挂的：

- 子账号把素材沉淀进**团队内部库**：要主账号审批；
- 子账号把团队项目里的素材投到**全网公开的素材广场**：不需要主账号知情，直接进 admin 队列（`canContributeToPlaza`：sub 在项目库可直接发起；通知也只发给投稿人本人，主账号全程无感知）；
- admin 审的是平台侧质量/合规，替代不了团队侧把关——项目里可能有客户素材、未发布剧目的角色设定，子账号一键就能公开出去；
- 另外 `permission.ts` L193-196 注释自己写了绕路：子账号想把**团队库**资产投广场，走"复用到项目→沉淀申请"即可。既然团队库的口子封了还有绕路，这个限制只剩摩擦、没有控制。

**建议**：子账号投广场改为两级审（先主账号、后 admin），或至少在投稿时通知主账号并允许其撤回。规则定了之后，团队库入口封不封就顺理成章了。

---

## 二、P1 逻辑缺口（不自相矛盾，但会在真实使用中出问题)

1. **审批方收不到"有新申请"的通知**。通知只在审核结果产生时发给申请人；主账号/admin 必须主动逛审核中心才知道有活儿（`NotificationBell` 只按 toUserId 过滤已有通知）。应在申请提交时给审批方发通知/角标。
2. **重复申请无防护**。同一资产可以连提 N 条沉淀申请/广场投稿，全部 pending。第二次批准会被同名去重挡下（兜底存在），但审批列表会被垃圾申请淹没。提交时应挡"该资产已有待审申请"。
3. **库内同名唯一 vs 工作流自动落库是冲突的**。`libraryHasSameName` 让"同库同名"直接失败——手动场景没问题，但你说工作流拆解产物"默认直接落项目库"：AI 拆出第二个「古镇街道」时自动落库就会静默失败。工作流接进来之前要定：自动落库遇同名是自动加后缀、合并为子资产、还是进"待整理"暂存区。
4. **只有"直接复用"能选择带不带音色**。`directReuse` 有 `includeVoice` 参数和 UI 勾选；收藏/复用/沉淀都无条件深拷贝 voice（`cloneForCopy` L64）。要么都给选择权，要么都不给，现在的差异用户无法理解。
5. **广场没有同名约束**。团队库/项目库同名会挡，`materializePlaza` 不查——广场可以堆无数个「古镇街道」。公共货架可以允许重名，但搜索/引用体验要有对策（作者署名、上架时间等区分）。
6. **store 层多个写操作没有权限守卫**。`setCover / setVoice / clearVoice / renameCanvas / deleteCanvas` 直接改 world，规则只在 UI 层（`canEditCover` 等）。与"规则原样搬后端"的架构承诺不符，后端化时必须补齐。
7. **复刻音色是 objectURL 占位**。刷新即失效；且副本拷贝 voice 时把失效 URL 一起拷走。Demo 可接受，但要在接后端清单里标注"音色样本必须上传成持久存储"。
8. **占位图能当成品流转**。`addLook` 新造型直接 `status:'done'` + 占位图，随后可被沉淀、贡献、甚至 admin 审核通过上架到广场——上架的是一张占位 SVG。"只有 done 才能流转"的红线被占位 done 绕过了。生成中应有 `generating` 态并挡流转。
9. **子账号退场没有定义**。子账号被移出项目/被删除后，它的 pending 申请、它贡献到广场的素材（contributedBy 指向它）归属如何处理，目前无规则。

---

## 三、你提的三个待决问题：我的建议

### Q1：图片删光了，资产怎么办？

先对齐一个现状：demo 里其实**删不到"光"**——素模不能单删（素模上的垃圾桶=删整个资产），非角色资产也只有"删整个"和"删子样式"两种。所以真正的问题是"**删到只剩最后一张图时，删除动作的语义是什么**"。

我的建议是**分层定语义**，而不是在 (a)/(b) 里二选一：

- **团队库 / 广场（货架层）**：货架上只该有成品。删到最后一张 = 删除整个资产，用你的方案 (a)：二次确认，文案讲清后果——"这是该资产的最后一张图片，继续将删除整个资产（含提示词、音色）。已复用出去的副本不受影响。" 不保留空壳。
- **项目库（工作层）**：这里方案 (b) 的"空壳"反而是对的，而且你们的数据模型早就准备好了——`types.ts` 里 `AssetStatus` 明确写着 **"资产成不成立看状态，不看有没有图"**，`empty` 态就是为此设计的。删光图片 → 资产降级为 `empty` 壳，保留名字 + 提示词 + 音色 + 血缘；`empty` 不是 `done`，红线自动保证它不能被复用/沉淀/贡献，不会污染货架。空壳卡片给明确的空态视觉 + "继续生成"入口。
- 交互上，两种场景都在删最后一张时弹确认，只是项目库的确认给两个选项："仅删图片，保留设定（可稍后重新生成）" / "连同资产一起删除"。

这样 (a)、(b) 各归其位：货架干净、工作层不丢用户的提示词心血。

### Q2：资产为空时"+"支持复用其他资产

支持，这是对的方向，而且和上面的 `empty` 壳设计正好闭环。建议空态"+"给三个入口：

1. **AI 生成**（沿用现有提示词面板，提示词已在壳上，一键续生成）；
2. **从资产库复用**（你们已有"从资产库选择并复用"的逻辑，选择器里三层可选，走现有 directReuse/reuse，权限天然复用——子账号只能看到被分配项目/团队库/广场）；
3. **本地上传**。

两个要注意的点：复用进来会撞"库内同名唯一"（复用一个和空壳同名的资产会被挡，需要定义是"填充进这个壳"还是"新建一份"——我建议语义定为**填充**：把选中资产的图注入当前空壳，保留壳的身份和血缘记一笔 masterId，这样才叫"复用其他资产补这个位"，而不是又多出一份资产）；其二，工作流场景里这个"+"要能按类目过滤（角色位只能选角色）。

### Q3：画布音频保存——绑音色还是存音频？

先把概念钉死，交互自然就顺了：**音色（voice）是"能生成任意台词的声音身份"，音频资产是"一段具体的声音内容"**。画布上生成的音频绝大多数是台词配音/BGM——它们是内容，不是身份。把一段台词"绑定为音色"，真实含义是**拿它当复刻样本去克隆一个音色**。

所以建议保存弹窗第一步不要问"绑音色还是传音频"（用户分不清），而是问用途：

```
把这段音频保存为——
┌─────────────────────────────┐
│ 🎵 音频素材                   │  台词、BGM、音效，存入项目资产库
│    （可选：关联到某个角色）      │
├─────────────────────────────┤
│ 🎤 角色音色                   │  用这段声音克隆音色，作为角色的声音
│    需 5–10s 清晰单人干声        │
└─────────────────────────────┘
```

- 选**音频素材**：走现有 runSaveToProject 落 audio 类目；增加一个可选的"关联角色"标签（音频资产加个 `roleId`/tag），台词配音天然属于某个角色，将来检索和自动配音都用得上。这个"关联"取代现在语义不明的音频「关联已有」。
- 选**角色音色**：第二步选角色（本项目角色列表，与"关联已有角色"同一交互）→ 前置校验时长/单人声 → 复用现有克隆音色流程（`AssetDetail` 的 voice picker 已有完整实现，等于把入口从详情页搬到画布右键）。角色已有音色时提示"将替换现有音色「××」"（一角一音色的现有规则）。
- 一个防呆：如果这段音频本来就是用某角色的音色生成的（节点血缘可判断），选"克隆音色"是循环引用，应提示"这段音频由「××」的音色生成，无需再克隆"。
- 模型层建议：让 `Voice.sampleUrl` 直接引用一份 audio 资产的 id，而不是另存一份文件——音色样本本身就是一段音频资产，两套存储将来必然出同步问题。

---

## 四、口径小错位（你描述的规则 vs demo 实际行为）

| # | 你的描述 | Demo 实际 | 建议 |
|---|---------|----------|------|
| 1 | 封面不选则"默认第一张" | 回落到**素模**（`coverOf`: cover → baseModel），非角色回落自己那张图 | 素模回落更合理，把产品文档口径改过来即可 |
| 2 | 服装"新增其他样式" | 画布面板把服装排除在"多样式品类"外（`STYLE_CATS` 无 costume，单图直接"使用"），但详情页和"关联已有"又允许服装挂样式 | 服装要不要多样式，定一个口径统一三处 |
| 3 | "子账号贡献到素材广场"在项目详情发起 | 属实，但主账号全程无感知（见 P0-5） | 同 P0-5 |
| 4 | 工作流产物"默认直接落项目库" | WorkflowShell 是占位页，未实现；落库将撞同名唯一（见 P1-3） | 接工作流前先定同名策略 |
| 5 | 广场"最新/最热"排序 | 按钮是死的（`PlazaPage` L110-111 无 onClick） | Demo 可接受，标记待做 |
| 6 | 团队库/项目库"批量操作" | 只有选择态，无任何批量动作 | 同上；接批量删除时注意 P0-2/P0-3 |

---

## 五、建议的处理顺序

1. **先定规则再改 UI**：P0-3（删除权限）、P0-5（子账号投广场是否过主账号）是纯产品决策，一句话定完，代码改动很小；
2. **音频链路**（P0-1 + Q3）：决定本期做全还是收口，这是改动量最大的一块；
3. **删除体验**（P0-2 + Q1）：确认弹窗 + 按钮分离 + empty 壳语义，一次改完；
4. **审核快照化**（P0-4 + P1-1/2）：申请冻结快照、双向通知、防重复提交；
5. 其余 P1 项排进后端化 checklist（store 守卫、音色持久化、占位图流转）。
